# 发布交接状态

更新：2026-09-25。

- Sites 项目：`appgprj_6ab530f5fb78819180c3605eef299962`；访问模式为 public，但版本数为 0，没有已发布 URL。
- 本地 Git 可以正常读取。先前发布流程的“Git metadata is unreadable”实际由 sandbox 账户与用户账户不同引起的 dubious ownership 检查导致。无需删除或重新初始化 .git。
- 本次仅在发布进程中设置该项目的 safe.directory 后，版本检查通过；联网仍因 `git.chatgpt-team.site` 无法解析而失败。受限环境及获准的外部运行环境均已尝试，尚未推送或部署。
- GitHub 插件已连接账号 `353236501-max`，但可访问仓库列表为空，需要确定并授权目标仓库。Sites 源码仓库不是 GitHub 仓库。
- TypeScript 检查及 8 项核心测试本次通过。
- PDF 驱动的完整内容替换仍待实现：现有内置讲解和参数题模板不等同于已完成 PDF 内容替换；仅检索并展示 PDF 引用不足以视为完成。
- 公网版本无法直接读取这台电脑上的 PDF 服务。部署前必须明确资料服务的运行方式，不能声称上传前端就自动拥有本地知识库。

继续操作时保留现有文件与 Git 历史，不提交 .env、知识库原件、索引、个人记录或密钥，不重复初始化版本库。
