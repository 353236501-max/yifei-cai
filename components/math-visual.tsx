"use client";
import {ArrowUpRight,ChartSpline,Download} from 'lucide-react';
import {Button} from '@/components/ui/button';
import type {Topic} from '@/lib/curriculum';
export default function MathVisual({topic}:{topic:Topic}){function manim(){const code=`from manim import *\n\n# Original illustrative parabola; not a verification of the selected problem.\nclass GentleParabola(Scene):\n    def construct(self):\n        axes = Axes(x_range=[-3,3,1], y_range=[0,9,1])\n        curve = axes.plot(lambda x: x*x, color=GREEN)\n        label = MathTex(r"y=x^2").to_corner(UL)\n        self.play(Create(axes), Write(label))\n        self.play(Create(curve), run_time=3)\n        self.wait(2)\n`;const u=URL.createObjectURL(new Blob([code],{type:'text/plain'}));const a=document.createElement('a');a.href=u;a.download='gentle_parabola.py';a.click();setTimeout(()=>URL.revokeObjectURL(u),1000);}return <div className="small-card"><h3><ChartSpline size={18}/> 给理解一个画面</h3><p>{topic.graph}</p><a className="resource-link" target="_blank" rel="noreferrer" href="https://www.geogebra.org/calculator">打开 GeoGebra <ArrowUpRight size={15}/></a><a className="resource-link" target="_blank" rel="noreferrer" href="https://www.desmos.com/calculator">打开 Desmos <ArrowUpRight size={15}/></a><p className="muted">早觉雨大人，可以先画一个简单例子，再改变参数。外部工具按其官方服务条款使用。</p><Button variant="ghost" onClick={manim}><Download/> 下载 Manim 抛物线示例</Button><p className="muted">示例源码，尚未渲染；安装 Manim 后运行 manim -pql gentle_parabola.py GentleParabola。</p></div>;}
